import { Theme, type ThemeConfig, type CanvasOptions } from '../../core/src/index.ts';
export declare enum PopperShape {
    Star = "star",
    Circle = "circle"
}
export interface PopperConfig extends ThemeConfig {
    shape?: PopperShape;
    size?: number;
    numParticles?: number;
}
export declare class Popper implements Theme<PopperConfig> {
    #private;
    constructor({ shape, size, numParticles }?: PopperConfig, canvasOptions?: CanvasOptions);
    mount(el: HTMLCanvasElement): void;
    unmount(): void;
    private animate;
}
//# sourceMappingURL=popper.d.ts.map